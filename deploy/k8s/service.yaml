apiVersion: v1
kind: Service
metadata:
  name: fraud-aml-investigation-crew
  labels:
    app: fraud-aml-investigation-crew
spec:
  selector:
    app: fraud-aml-investigation-crew
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
